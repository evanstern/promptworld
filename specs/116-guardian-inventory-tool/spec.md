# Feature Specification: Guardian Inventory Tool

**Feature Branch**: `task-197-guardian-inventory-tool`

**Created**: 2026-08-03

**Status**: Stub (number claimed; specification in progress on the feature branch)

**Board Task**: TASK-197

**Input**: Board card TASK-197 — "Guardian inventory tool: let the Guardian read and change
what a villager carries, and make it look first."

## Stub

This directory exists on `main` to claim spec number 116 for TASK-197 before any
implementation commit, per the claim-before-work protocol (spec 065) and the stub-first
rule (TASK-195). `spec-bridge`'s gate derives a linked task's provable status by reading
`specs/NNN-*` off the filesystem at the repository root, so a spec that lives only on a
branch is invisible to it.

The full specification — overview, user scenarios, functional requirements, and the
plan/tasks/contracts artifacts beside it — is authored on `task-197-guardian-inventory-tool`
and lands with that task's single pull request.
